version: '3.8'

services:
  url-shortener:
    build: .
    container_name: url-shortener-app
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped